# プライバシーポリシー

**LMS Saver**（以下「本拡張」）のプライバシーポリシーです。最終更新: 2026-09-20

## 1. 収集するデータ

本拡張は**ユーザーの個人情報・学習データを開発者に収集・送信しません。**

処理の対象となるデータと、その行き先は以下の通りです:

| データ | 処理 | 保存先 | 開発者に見えるか |
|---|---|---|---|
| LMSの資料ファイル (PDF等) | 開いた資料のダウンロード・保存 | ユーザーのPC (`~/Documents/WebClass資料`) | ❌ 見えない |
| 授業名・回・ファイル名 | フォルダ分類のためのラベル付け | ユーザーのPC (台帳 `~/.lms_saver/`) | ❌ 見えない |
| 課題・資料のタイトルと利用可能期間 | Google Tasksへの登録 | **ユーザー自身の**Google Tasks | ❌ 見えない |

## 2. 第三者サービスとの通信

本拡張は以下のサービスとユーザーのブラウザから直接通信します。いずれも**ユーザー自身のアカウント**に対する操作で、データは開発者を経由しません:

- **金沢大学 WebClass LMS** (`lms-wc.el.kanazawa-u.ac.jp`) — 開いたページの取得・資料のダウンロード。大学アカウントのセッションcookieを使いますが、cookieを開発者に送る仕組みはありません
- **Google Tasks API** — ユーザーがOAuthで承認した場合のみ。本拡張が登録するのは「課題・資料のタイトル」「利用可能期間の終了日時」「元URL」です

## 3. Google OAuth について

- 本拡張は Google Tasks API (`https://www.googleapis.com/auth/tasks`) とメールアドレス確認 (`userinfo.email`) のみを要求します
- 取得したアクセストークンはブラウザ内に一時保持され、開発者に送信されません
- 承認はいつでも取り消せます: [Googleアカウント設定 → セキュリティ → サードパーティのアクセス](https://myaccount.google.com/permissions)

## 4. Native Messaging ホスト

ファイル保存のため、ブラウザ拡張はユーザーのPC上の補助プログラム (Python / PyInstallerバイナリ) と通信します。この通信は**PC内部のみ**で完結し、ネットワークを経由しません。ホストが保存するのはLMSのファイル本体とフォルダ分類用のメタデータのみです。

## 5. データの保持と削除

- 保存された資料・設定・台帳はすべてユーザーのPC内にあり、ユーザーがいつでも削除できます (`~/.lms_saver/` フォルダと保存先フォルダ)
- Google Tasksに登録されたタスクはユーザーのGoogleアカウント上にあるため、[Google Tasks](https://tasks.google.com/) からいつでも削除できます
- 拡張を削除してもPC内の保存ファイルは残ります (意図仕様)

## 6. データの共有

開発者はユーザーのいかなるデータも**受け取らず、保持せず、第三者に提供しません。**

## 7. お問い合わせ

本ポリシーについての質問は GitHub の Issues またはリポジトリ記載の連絡先まで。
